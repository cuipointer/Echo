# Extension:Camera(占位)

**状态**:**未落地**(placeholder)

## 预期范围

Camera 横向领域,跨 Desense / EMC / SAR 的通用 Camera 噪声与干扰策略。

## 与 core 的关系

当前 Camera 相关内容在 core 知识库 [knowledge/domain/camera/](../../knowledge/domain/camera/) 中。未来若发展出**跨多个测试大类的 Camera 通用策略**(例如 Camera OIS Driver 的 EMI 评估同时影响 Desense + EMC + SAR),再迁入本扩展。

## 不填充的理由(2026-04-27)

现阶段 Camera 相关知识足以用 core 承载,单独开扩展会造成双写。保留目录仅作为**架构扩展点标记**。
